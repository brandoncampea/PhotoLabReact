// Seed a super admin price list and items
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);

// Create a super price list
const superPriceListName = 'WHCC Default';

// Add all WHCC product sizes to the super price list with base cost
});

console.log('Super price list and items seeded.');
